name: Permanent Lightsail backup host diagnostic

on:
  pull_request:
    paths:
      - ".github/workflows/lightsail-backup-host-diagnostic.yml"
      - "scripts/production/lightsail/diagnose_backup_host.py"
      - "scripts/production/lightsail/test_diagnose_backup_host.py"
  push:
    branches: [main]
    paths:
      - ".github/workflows/lightsail-backup-host-diagnostic.yml"
      - "scripts/production/lightsail/diagnose_backup_host.py"
      - "scripts/production/lightsail/test_diagnose_backup_host.py"

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: actions/setup-python@v7
        with:
          python-version: "3.11.16"
      - name: Install locked controller dependencies
        run: python -m pip install --require-hashes -r scripts/rds_rehearsal/requirements.lock
      - name: Validate read-only backup diagnostic
        run: |
          python -m py_compile scripts/production/lightsail/diagnose_backup_host.py
          python -m unittest scripts.production.lightsail.test_diagnose_backup_host -v

  diagnose:
    needs: validate
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      id-token: write
    env:
      AWS_EC2_METADATA_DISABLED: "true"
      LIGHTSAIL_ADMIN_IPV4_CIDR: ${{ secrets.LIGHTSAIL_ADMIN_IPV4_CIDR }}
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: actions/setup-python@v7
        with:
          python-version: "3.11.16"
      - name: Install locked controller dependencies
        run: python -m pip install --require-hashes -r scripts/rds_rehearsal/requirements.lock
      - name: Assume read-only backup-host diagnostic boundary
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          role-session-name: qf-backup-host-diagnostic
          role-duration-seconds: 600
          aws-region: ca-central-1
          mask-aws-account-id: true
          unset-current-credentials: true
          inline-session-policy: >-
            {"Version":"2012-10-17","Statement":[{"Effect":"Allow","Action":[
              "lightsail:GetInstance","lightsail:GetStaticIp","lightsail:GetInstancePortStates",
              "lightsail:OpenInstancePublicPorts","lightsail:CloseInstancePublicPorts","lightsail:GetInstanceAccessDetails"
            ],"Resource":"*"}]}
      - name: Inspect first-backup host state without changes
        run: python -m scripts.production.lightsail.diagnose_backup_host
      - name: Publish sanitized backup-host diagnostic
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: permanent-lightsail-backup-host-diagnostic
          path: lightsail-backup-host-diagnostic/summary.json
          if-no-files-found: error
          retention-days: 14
